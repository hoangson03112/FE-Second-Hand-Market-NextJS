import { PageLoader } from "@/components/ui";

export default function Loading() {
  return (
    <PageLoader
      eyebrow="Eco Market"
      title="Đang chuẩn bị nội dung cho bạn."
      caption="Chỉ mất một khoảnh khắc."
    />
  );
}
